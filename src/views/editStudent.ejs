<!DOCTYPE html>
<%- include('layout/header', {title: 'Editando Usuário' }); %>

    <html lang="pt-br">

    <body class="bg-dark-subtle">
        <%- include('layout/navbar', {loggedUser}); %>

            <div class="container-md">
                <div class="center-box">
                    <div class=" border border-black mt-5">
                        <h2 class="text-center text-primary mb-3 mt-3" style="font-size: 1em;">
                            Editando o estudante <b>
                                <%= student.name %>
                            </b>
                        </h2>
                        <form action="/students/<%= student.id %>" method="post">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                            <%- include('layout/formStudent', {student, users, edit: true}) %>

                                <div class="form-group  d-flex justify-content-end">
                                    <a class=" pr-2" href="/students">
                                        <button type="button" class="btn btn-link mb-3 mt-3">Voltar</button>
                                    </a>
                                    <button style="margin-left: 5px;" type="submit"
                                        class="btn btn-success mb-3 mt-3">Salvar</button>
                                </div>
                                <div class="form-group  d-flex justify-content-end">
                                </div>
                        </form>
                    </div>

                </div>

            </div>
    </body>

    </html>